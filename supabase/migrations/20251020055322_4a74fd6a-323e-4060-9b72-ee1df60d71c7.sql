-- Delete all Direct Labor records from the database

-- Delete from dl_roles (Direct Labor roles in models)
DELETE FROM dl_roles;

-- Delete from dl_roles_library (Direct Labor roles library)
DELETE FROM dl_roles_library;
